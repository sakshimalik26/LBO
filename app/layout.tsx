import type {Metadata} from 'next';import './globals.css';export const metadata:Metadata={title:'LBO Lab — CedarPack',description:'Build your first leveraged buyout model. A two-hour guided exercise in INR crore.'};export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
